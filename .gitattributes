# Shell-Skripte und Docker-Dateien immer mit LF auschecken (laufen auf Linux)
*.sh text eol=lf
Dockerfile text eol=lf
*.yml text eol=lf

# Fertige Bibliotheken unveraendert lassen (keine Zeilenenden-Umwandlung)
*.min.js -text
